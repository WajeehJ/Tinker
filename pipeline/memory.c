#include "memory.h"
#include "pipe_base.h"
#include "processor.h" 

void memory_operation() {
    if(memory_stage->memory_used) {
        if(memory_stage->read) {

        } else if(memory_stage->write) {
            
        }
    }
}
